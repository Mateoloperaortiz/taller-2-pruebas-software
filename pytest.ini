[pytest]
testpaths = .
pythonpath = .
addopts = --strict-markers
markers =
    external: requiere acceso real a JSONPlaceholder
    integration: integra componentes con SQLite real
    unit: lógica aislada sin red ni base de datos real
